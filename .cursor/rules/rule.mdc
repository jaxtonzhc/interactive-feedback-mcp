---
description: Interactive Feedback MCP 使用规范
globs:
alwaysApply: true
---

# Interactive Feedback MCP 使用规范

## 核心规则

1. **强制调用**：每次回复结束前，必须调用 `interactive_feedback` 工具向用户确认结果并询问后续需求。
2. **提供选项**：调用时必须提供 `predefined_options` 预定义选项供用户快速选择，最后一项为结束选项。
3. **禁止自行结束**：未调用 `interactive_feedback` 不得结束回复。仅当用户明确选择结束选项时才停止。
4. **信息传递**：调用时传递 `project_path`、`project_name`、`category`、`priority` 等上下文参数。

## 调用示例

```python
interactive_feedback(
    message="已完成 XX 功能。你还需要什么？",
    predefined_options=["继续优化", "查看其他模块", "结束本轮交互"],
    project_path="/path/to/project",
    project_name="my-project",
    priority=3,
    category="feature"
)
```

## 分类参考

- `category`: bug | feature | review | performance | docs | test | deploy | other
- `priority`: 1(最低) - 5(最高)，默认 3
