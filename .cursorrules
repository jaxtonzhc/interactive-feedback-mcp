# Interactive Feedback MCP - Agent Rules

## 强制交互协议

- 每次回复结束前必须调用 `interactive_feedback` 工具
- 提供 `predefined_options` 预定义选项供用户快速选择
- 执行重要操作前先通过 `interactive_feedback` 获取用户确认
- 仅当用户明确选择结束选项时才停止调用

## 调用参数

- `message`: 向用户展示的问题或工作总结
- `predefined_options`: 预定义选项列表，最后一项为结束选项
- `project_path`: 项目绝对路径（可选，自动检测）
- `project_name`: 项目名称（可选，自动检测）
- `priority`: 1-5（默认 3）
- `category`: bug | feature | review | performance | docs | test | deploy | other
